services:
  # Servidor web Nginx
  nginx:
    image: nginx:alpine
    container_name: paicat_nginx_manual
    ports:
      - "80:80"
    volumes:
      - ./:/var/www/html:cached
      - ./docker/nginx/default.conf:/etc/nginx/conf.d/default.conf:ro
    depends_on:
      - php
    networks:
      - paicat_network_manual
    restart: unless-stopped

  # PHP-FPM 8.2 - SIN entrypoint automático
  php:
    build:
      context: .
      dockerfile: docker/php/Dockerfile.manual
    container_name: paicat_php_manual
    volumes:
      - ./:/var/www/html:cached
    environment:
      - DB_CONNECTION=paicat
      - DB_HOST=mariadb
      - DB_PORT=3306
      - DB_DATABASE=paicat
      - DB_USERNAME=laravel
      - DB_PASSWORD=secret
      - PHP_OPCACHE_ENABLE=1
      - PHP_OPCACHE_VALIDATE_TIMESTAMPS=1
      - REDIS_HOST=redis
      - REDIS_PORT=6379
      - CACHE_DRIVER=redis
      - SESSION_DRIVER=redis
      - QUEUE_CONNECTION=redis
    depends_on:
      mariadb:
        condition: service_healthy
      redis:
        condition: service_started
    networks:
      - paicat_network_manual
    restart: unless-stopped

  # Vite Dev Server (HMR para desarrollo)
  vite:
    image: node:20-alpine
    container_name: paicat_vite_manual
    working_dir: /var/www/html
    ports:
      - "5173:5173"
    volumes:
      - ./:/var/www/html:cached
      - vite_node_modules_manual:/var/www/html/node_modules
    command: sh -c "npm install && npm run dev -- --host 0.0.0.0"
    networks:
      - paicat_network_manual
    restart: unless-stopped
    depends_on:
      - php

  # MariaDB 11.7
  mariadb:
    image: mariadb:11.7
    container_name: paicat_mariadb_manual
    ports:
      - "3308:3306"
    environment:
      MYSQL_ROOT_PASSWORD: root
      MYSQL_DATABASE: paicat
      MYSQL_USER: laravel
      MYSQL_PASSWORD: secret
      MARIADB_CHARACTER_SET_SERVER: utf8mb4
      MARIADB_COLLATE_SERVER: utf8mb4_unicode_ci
    command: >
      --innodb-buffer-pool-size=512M
      --innodb-log-file-size=128M
      --innodb-flush-log-at-trx-commit=2
      --innodb-flush-method=O_DIRECT
      --query-cache-type=1
      --query-cache-size=64M
      --max-connections=200
      --table-open-cache=4000
      --thread-cache-size=50
    volumes:
      - mariadb_data_manual:/var/lib/mysql
      - ./docker/mariadb/01-init-permissions.sql:/docker-entrypoint-initdb.d/01-init-permissions.sql:ro
      - ./datos_paicat_completo.sql:/docker-entrypoint-initdb.d/02-datos.sql:ro
    healthcheck:
      test: ["CMD", "healthcheck.sh", "--connect", "--innodb_initialized"]
      interval: 10s
      timeout: 5s
      retries: 5
    networks:
      - paicat_network_manual
    restart: unless-stopped

  # Redis para cache y queues
  redis:
    image: redis:7-alpine
    container_name: paicat_redis_manual
    ports:
      - "6379:6379"
    volumes:
      - redis_data_manual:/data
    networks:
      - paicat_network_manual
    restart: unless-stopped

  # PHPMyAdmin
  phpmyadmin:
    image: phpmyadmin:latest
    container_name: paicat_phpmyadmin_manual
    ports:
      - "8081:80"
    environment:
      PMA_HOST: mariadb
      PMA_PORT: 3306
      PMA_USER: root
      PMA_PASSWORD: root
      UPLOAD_LIMIT: 100M
    depends_on:
      - mariadb
    networks:
      - paicat_network_manual
    restart: unless-stopped

  # Mailhog
  mailhog:
    image: mailhog/mailhog:latest
    container_name: paicat_mailhog_manual
    ports:
      - "1025:1025" # SMTP
      - "8025:8025" # Web UI
    networks:
      - paicat_network_manual
    restart: unless-stopped

networks:
  paicat_network_manual:
    driver: bridge

volumes:
  mariadb_data_manual:
    driver: local
  redis_data_manual:
    driver: local
  vite_node_modules_manual:
    driver: local
