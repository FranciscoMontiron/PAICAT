# Ejemplo de docker-compose.override.yml para desarrollo local
# Copia este archivo a docker-compose.override.yml y personaliza según necesites
# Este archivo NO se incluye en el repositorio (.gitignore)

version: '3.8'

services:
  php:
    # Descomentar para debugging con Xdebug
    # environment:
    #   - PHP_IDE_CONFIG=serverName=PAICAT
    #   - XDEBUG_MODE=debug
    #   - XDEBUG_CONFIG=client_host=host.docker.internal client_port=9003

    # Volumen adicional para development
    volumes:
      - ./:/var/www/html

  # Descomentar para usar PostgreSQL en lugar de MariaDB
  # postgres:
  #   image: postgres:16-alpine
  #   container_name: paicat_postgres
  #   restart: unless-stopped
  #   environment:
  #     POSTGRES_DB: paicat
  #     POSTGRES_USER: laravel
  #     POSTGRES_PASSWORD: secret
  #   ports:
  #     - "5432:5432"
  #   volumes:
  #     - postgres_data:/var/lib/postgresql/data
  #   networks:
  #     - paicat_network

  # Descomentar para agregar Elasticsearch
  # elasticsearch:
  #   image: docker.elastic.co/elasticsearch/elasticsearch:8.11.0
  #   container_name: paicat_elasticsearch
  #   environment:
  #     - discovery.type=single-node
  #     - "ES_JAVA_OPTS=-Xms512m -Xmx512m"
  #     - xpack.security.enabled=false
  #   ports:
  #     - "9200:9200"
  #   volumes:
  #     - elasticsearch_data:/usr/share/elasticsearch/data
  #   networks:
  #     - paicat_network

# volumes:
#   postgres_data:
#   elasticsearch_data:
